# Contributing

Create focused changes, keep fictional seed data obviously fictional, never commit secrets, and add tests for behavior changes. Run backend tests and lint plus the Android debug build/tests when their toolchains are available. Record schema changes through Alembic. Preserve OpenStreetMap and tile-provider attribution in every map surface.

